# Server Configuration
server.port=8081
server.servlet.context-path=/

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/voting_system?useSSL=false&serverTimezone=UTC
spring.datasource.username=khaleel
spring.datasource.password=abdulkhaleel@2004
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect
spring.jpa.show-sql=true

# JWT Configuration
app.jwtSecret=YourJwtSecretKey
app.jwtExpirationInMs=86400000

# Logging Configuration
logging.level.org.springframework=INFO
logging.level.com.voting=DEBUG

# File Storage Configuration
blockchain.storage.path=./blockchain_data
